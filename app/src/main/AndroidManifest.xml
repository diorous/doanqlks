<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diorous.quanlykhachsan">

    <application
        android:name=".Application.HotelApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity"></activity>
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.SearchActivity"></activity>
        <activity android:name=".Activity.CreatePhieuDatPhongActivity" />
        <activity android:name=".Activity.CreateDanhMucPhongActivity" />
        <activity android:name=".Activity.QuanlyPhongActivity" />
        <activity android:name=".Activity.CreatePhongActivity" />

    </application>

</manifest>